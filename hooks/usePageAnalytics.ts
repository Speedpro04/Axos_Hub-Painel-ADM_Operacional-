import { useRef, useEffect } from 'react'

export const usePageAnalytics = (_pageName: string, _metadata?: Record<string, any>) => {
  // Placeholder - tracking removido ate backend estar pronto
  // TODO: Implementar tracking com Supabase quando necessario
  useEffect(() => {}, [])
}
